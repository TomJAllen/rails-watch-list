<h1>Create a new list!</h1>

<%= simple_form_for(@list) do |f| %>
  <div class="form-inputs">
    <%= f.input :name, :label => 'What do you want to call the list?' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit%>
  </div>
<% end %>

<%= link_to "<- back", lists_path%>
